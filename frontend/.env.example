# =============================================================================
# Frontend env templates
# Vite loads by mode:
#   npm run dev          → .env.development (copy from .env.development.example)
#   npm run build        → .env.production  (copy from .env.production.example)
# =============================================================================

# See .env.development.example for local dev
# See .env.production.example for production builds
